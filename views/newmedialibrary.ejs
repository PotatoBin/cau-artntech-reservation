<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <title>중앙대학교 예술공학대학 대여시스템</title>
  <meta name="description" content="New Media Library 대여 현황">
  <meta name="keywords" content="중앙대학교, 예술공학대학, 대여, 예약, 학생회">
  <meta property="og:title" content="중앙대학교 예술공학대학 대여시스템">
  <meta property="og:description" content="New Media Library 대여 현황">
  <meta property="og:image" content="img/logo.jpg">
  <meta property="og:url" content="https://cauartntech.com/view/newmedialibrary">
  
  <link rel="icon" href="img/favicon.ico" type="image/x-icon" />

  <!-- Google Fonts: Noto Sans KR -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@100..900&display=swap" rel="stylesheet">
  
  <style>
    @font-face {
      font-family: 'HSYeoleum20-Regular';
      src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/2405@1.0/HSYeoleum20-Regular.woff2') format('woff2');
      font-weight: normal;
      font-style: normal;
    }
    .custom-font {
      font-family: 'HSYeoleum20-Regular';
    }

    .noto-sans-kr-regular {
      font-family: "Noto Sans KR", serif;
      font-optical-sizing: auto;
      font-weight: 400;
      font-style: normal;
    }
    html {
      font-size: 14px;
    }
    body {
      font-family: "Noto Sans KR", serif;
      background-color: #232323;
      color: #ffffff;
      margin: 0;
      padding: 0;
    }
    .container {
      width: 90%;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    .banner {
      width: 100%;
      aspect-ratio: 2 / 1;
      overflow: hidden;
      margin-bottom: 20px;
    }
    .banner img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
    /* header 영역 (좌/중앙/우) 정렬 */
    header .header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 30px;
    }
    .nav-button {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 40px;
      height: 40px;
      color: #ffffff;
      text-decoration: none;
    }
    .nav-button svg {
      width: 100%;
      height: 100%;
    }
    .header-title {
      flex: 1;
      text-align: center;
    }
    .header-title h1 {
      font-size: clamp(1.8em, 5vw, 2.5em);
      white-space: nowrap;
      overflow: hidden;
      margin: 0;
    }
    .header-title h2 {
      font-size: clamp(1em, 3vw, 1.2em);
      white-space: nowrap;
      overflow: hidden;
      margin: 5px 0 0;
      color: #bbb;
    }
    hr {
      border: 0;
      height: 1px;
      background: #666;
      margin: 20px 0;
    }
    .room-section {
      margin-bottom: 20px;
      /* 아래에서 위로 올라오는 애니메이션 */
      opacity: 0;
      animation: fadeInUp 0.8s ease forwards;
      margin-top: 10px;
    }
    .room-box {
      display: inline-block;
      padding: 5px 10px;
      border-radius: 10px;
      font-size: 1.1em;
      font-weight: bold;
      margin-bottom: 10px;
    }
    .room-blue {
      background-color: #2196F3;
      color: #ffffff;
    }
    .room-gray {
      background-color: #9E9E9E;
      color: #ffffff;
    }
    .room-silver {
      background-color: #B0B0B0;
      color: #ffffff;
    }
    .room-gold {
      background-color: #FFD700;
      color: #ffffff;
    }
    .reservation-box {
      background: #333;
      border: 1px solid #444;
      border-radius: 10px;
      padding: 10px;
      margin-top: 5px;
      margin-bottom: 10px;
    }
    .reservation-table {
      width: 100%;
      border-collapse: collapse;
    }
    .reservation-table td {
      padding: 5px 10px;
      border-bottom: 1px solid #444;
      font-size: 0.9em;
      color: #bbb;
    }
    .reservation-table tr:last-child td {
      border-bottom: none;
    }
    .footer {
      margin-top: 20px;
      font-size: 0.9em;
      text-align: center;
      color: #bbb;
    }
    .footer a {
      color: #ffffff;
      text-decoration: none;
    }
    /* 애니메이션 keyframes */
    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body class="noto-sans-kr-regular">
  <!-- 시맨틱 구조: header / main / footer -->
  <header>
    <div class="container">
      <div class="banner">
        <!-- Lazy Loading 적용 -->
        <img src="/img/1.jpg" alt="Banner" loading="lazy">
      </div>
      <div class="header-inner">
        <!-- 좌측: 뒤로가기 버튼 -->
        <a href="https://www.cauartntech.com/view" class="nav-button" title="뒤로가기">
          <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M6 12.5a.5.5 0 0 0 .5.5h8a.5.5 0 0 0 .5-.5v-9a.5.5 0 0 0-.5-.5h-8a.5.5 0 0 0-.5.5v2a.5.5 0 0 1-1 0v-2A1.5 1.5 0 0 1 6.5 2h8A1.5 1.5 0 0 1 16 3.5v9a1.5 1.5 0 0 1-1.5 1.5h-8A1.5 1.5 0 0 1 5 12.5v-2a.5.5 0 0 1 1 0z"/>
            <path fill-rule="evenodd" d="M.146 8.354a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L1.707 7.5H10.5a.5.5 0 0 1 0 1H1.707l2.147 2.146a.5.5 0 0 1-.708.708z"/>
          </svg>
        </a>
        
        <!-- 중앙: 헤더 제목 -->
        <div class="header-title">
          <h1>New Media Library</h1>
          <h2>금일 예약 현황 (<%= today %>)</h2>
        </div>
        
        <!-- 우측: 새로고침 버튼 -->
        <a href="#" class="nav-button" title="새로고침" onclick="location.reload(); return false;">
          <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M8 3a5 5 0 1 0 4.546 2.914.5.5 0 0 1 .908-.417A6 6 0 1 1 8 2z"/>
            <path d="M8 4.466V.534a.25.25 0 0 1 .41-.192l2.36 1.966c.12.1.12.284 0 .384L8.41 4.658A.25.25 0 0 1 8 4.466"/>
          </svg>
        </a>
      </div>
    </div>
  </header>

  <main>
    <div class="container">
      <hr>
      <!-- 예약 섹션: 예시로 각 방(Blue, Gray, Silver, Gold) 예약 정보를 표로 보여줌 -->
      <!-- 01 Blue 예약 섹션 -->
      <section class="room-section">
        <div class="room-box room-blue">01 Blue</div>
        <div class="reservation-box">
          <table class="reservation-table">
            <tbody>
              <% if (reservations["01blue"] && reservations["01blue"].length > 0) { %>
                <% reservations["01blue"].forEach(reservation => { %>
                  <tr>
                    <td><%= reservation.time %></td>
                    <td><%= reservation.code %></td>
                    <td><%= reservation.name %></td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </section>

      <!-- 02 Gray 예약 섹션 -->
      <section class="room-section">
        <div class="room-box room-gray">02 Gray</div>
        <div class="reservation-box">
          <table class="reservation-table">
            <tbody>
              <% if (reservations["02gray"] && reservations["02gray"].length > 0) { %>
                <% reservations["02gray"].forEach(reservation => { %>
                  <tr>
                    <td><%= reservation.time %></td>
                    <td><%= reservation.code %></td>
                    <td><%= reservation.name %></td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </section>

      <!-- 03 Silver 예약 섹션 -->
      <section class="room-section">
        <div class="room-box room-silver">03 Silver</div>
        <div class="reservation-box">
          <table class="reservation-table">
            <tbody>
              <% if (reservations["03silver"] && reservations["03silver"].length > 0) { %>
                <% reservations["03silver"].forEach(reservation => { %>
                  <tr>
                    <td><%= reservation.time %></td>
                    <td><%= reservation.code %></td>
                    <td><%= reservation.name %></td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </section>

      <!-- 04 Gold 예약 섹션 -->
      <section class="room-section">
        <div class="room-box room-gold">04 Gold</div>
        <div class="reservation-box">
          <table class="reservation-table">
            <tbody>
              <% if (reservations["04gold"] && reservations["04gold"].length > 0) { %>
                <% reservations["04gold"].forEach(reservation => { %>
                  <tr>
                    <td><%= reservation.time %></td>
                    <td><%= reservation.code %></td>
                    <td><%= reservation.name %></td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </section>
    </div>
  </main>

  <footer class="footer">
    <div class="container">
      <hr>
      <p>
        ©중앙대학교 예술공학대학 학생회 | 
        <a href="https://pf.kakao.com/_TzxirG">문의사항</a>
      </p>
      <p>
        Created by 
        <a href="https://www.instagram.com/team.doob?igsh=MXNxdnJpaDgzengwZQ%3D%3D&utm_source=qr" class="custom-font">
          d{OO}b
        </a>
      </p>
    </div>
  </footer>
</body>
</html>
