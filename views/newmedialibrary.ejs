<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Media Library 대여 현황</title>
  <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />

  <!-- Google Fonts: Noto Sans KR -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@100..900&display=swap"
    rel="stylesheet"
  />

  <!-- 공통 CSS 파일 -->
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body class="noto-sans-kr-regular">
  <div class="container">
    <!-- 배너 이미지 -->
    <div class="banner">
      <img src="/img/1.jpg" alt="Banner">
    </div>
    
    <header>
      <!-- 뒤로가기 버튼 (좌측) -->
      <a href="https://www.cauartntech.com/view" class="nav-button" title="뒤로가기">
        <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 16 16">
          <path fill-rule="evenodd"
            d="M6 12.5a.5.5 0 0 0 .5.5h8a.5.5 0 0 0 
               .5-.5v-9a.5.5 0 0 0-.5-.5h-8a.5.5 0 0 
               0-.5.5v2a.5.5 0 0 1-1 0v-2A1.5 1.5 0 0 
               1 6.5 2h8A1.5 1.5 0 0 1 16 3.5v9a1.5 
               1.5 0 0 1-1.5 1.5h-8A1.5 1.5 0 0 
               1 5 12.5v-2a.5.5 0 0 1 1 0z" />
          <path fill-rule="evenodd"
            d="M.146 8.354a.5.5 0 0 1 
               0-.708l3-3a.5.5 0 1 1 .708.708L1.707 
               7.5H10.5a.5.5 0 0 1 0 1H1.707l2.147 
               2.146a.5.5 0 0 1-.708.708z" />
        </svg>
      </a>
      
      <!-- 헤더 제목 (중앙) -->
      <div class="header-title">
        <h1>New Media Library</h1>
        <h2>금일 예약 현황 (<%= today %>)</h2>
      </div>
      
      <!-- 새로고침 버튼 (우측) -->
      <a href="#" class="nav-button" title="새로고침" onclick="location.reload(); return false;">
        <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 16 16">
          <path fill-rule="evenodd"
            d="M8 3a5 5 0 1 0 4.546 2.914.5.5 
               0 0 1 .908-.417A6 6 0 1 1 8 2z" />
          <path
            d="M8 4.466V.534a.25.25 0 0 
               1 .41-.192l2.36 1.966c.12.1.12.284 
               0 .384L8.41 4.658A.25.25 0 0 1 8 
               4.466" />
        </svg>
      </a>
    </header>
    
    <hr>
    
    <!-- 01 Blue 예약 섹션 -->
    <section class="room-section">
      <div class="room-box room-blue">01 Blue</div>
      <div class="reservation-box">
        <table class="reservation-table">
          <tbody>
            <% if (reservations["01blue"] && reservations["01blue"].length > 0) { %>
              <% reservations["01blue"].forEach(reservation => { %>
                <tr>
                  <td><%= reservation.time %></td>
                  <td><%= reservation.code %></td>
                  <td><%= reservation.name %></td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
    
    <!-- 02 Gray 예약 섹션 -->
    <section class="room-section">
      <div class="room-box room-gray">02 Gray</div>
      <div class="reservation-box">
        <table class="reservation-table">
          <tbody>
            <% if (reservations["02gray"] && reservations["02gray"].length > 0) { %>
              <% reservations["02gray"].forEach(reservation => { %>
                <tr>
                  <td><%= reservation.time %></td>
                  <td><%= reservation.code %></td>
                  <td><%= reservation.name %></td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
    
    <!-- 03 Silver 예약 섹션 -->
    <section class="room-section">
      <div class="room-box room-silver">03 Silver</div>
      <div class="reservation-box">
        <table class="reservation-table">
          <tbody>
            <% if (reservations["03silver"] && reservations["03silver"].length > 0) { %>
              <% reservations["03silver"].forEach(reservation => { %>
                <tr>
                  <td><%= reservation.time %></td>
                  <td><%= reservation.code %></td>
                  <td><%= reservation.name %></td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
    
    <!-- 04 Gold 예약 섹션 -->
    <section class="room-section">
      <div class="room-box room-gold">04 Gold</div>
      <div class="reservation-box">
        <table class="reservation-table">
          <tbody>
            <% if (reservations["04gold"] && reservations["04gold"].length > 0) { %>
              <% reservations["04gold"].forEach(reservation => { %>
                <tr>
                  <td><%= reservation.time %></td>
                  <td><%= reservation.code %></td>
                  <td><%= reservation.name %></td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="3" style="text-align:center; color:#bbb;">예약 없음</td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>
    
    <div class="footer">
      <hr>
      <p>
        ©중앙대학교 예술공학대학 학생회 |
        <a href="https://pf.kakao.com/_TzxirG">문의사항</a>
      </p>
      <p>
        Created by
        <a
          href="https://www.instagram.com/team.doob?igsh=MXNxdnJpaDgzengwZQ%3D%3D&utm_source=qr"
        >
          Team d{OO}b
        </a>
      </p>
    </div>
  </div>
</body>
</html>
